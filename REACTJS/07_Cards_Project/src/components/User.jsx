import React from 'react'

const User = (props) => {

  // console.log(props);

  return (
    <div className='main'>
      Kanav, {props.name} 
      {/* {props.name} */}
    </div>
  )
}

export default User